# Idea Ledger

Last updated: 2026-06-05T00:00:00Z
Sentinel baseline: NONE (no sentinel report in `.ai-state/sentinel_reports/`; hackathon ideation seated promethean directly with pre-gathered `EXTERNAL_INSPIRATION.md`)

## Pending

Nine hackathon ideas proposed (3 per track). All scoped to the harness lever (SIA-H) only — SIA-W is not implemented in this repo. See `.ai-work/hackathon-tracks/IDEA_PROPOSAL.md`.

### Applied AI Track
- **A1 — Regex-Golf SIA**: self-improving structured-info-extraction task; new task via `--task_dir`, F1 verifier. (safe)
- **A2 — SIA-for-SQL** *(top pick)*: NL→SQL with execution-based verifier; catch Feedback-Agent inventing SQL self-repair. (high-ceiling)
- **A3 — SIA Triages Itself**: self-improving classifier over SIA's own failure logs; reflexive framing, self-sourced data. (differentiated)

### Framework Enhancement Track
- **F1 — Archive Mode** *(top pick)*: best-parent selection replacing linear chain; closes the DGM/AlphaEvolve population gap. Touches `orchestrator.py` + light `context_manager.py`. (high-ceiling)
- **F2 — Cost-Aware Loop**: cost-per-accuracy-point reporting + `--cost-budget` stop; token fields already flow through `_extract_metrics`. (safe)
- **F3 — Parallel Fan-out**: beam-width-K candidate scaffolds per generation, keep best. (differentiated)

### Research Track
- **R1 — Held-Out Verifier** *(top pick)*: measure coupled co-evolutionary Goodhart gap (paper §5) with a second grader. (high-ceiling)
- **R2 — Improvement Taxonomy**: classify Feedback-Agent edits to test RQ2a's SE-hygiene-vs-domain-reasoning claim; post-hoc, zero `sia/` edits. (safe)
- **R3 — Generation-Efficiency Curve**: characterize monotonicity / plateau / regression of harness-only SIA across tasks. (differentiated)

## Discarded

- **TriMul CUDA task** — needs H100/Modal; breaks 1-day + SIA-W constraints.
- **scRNA-seq denoising task** — heavy deps, slow eval; not demoable in a day.
- **Any weight-update (LoRA/RL) idea** — SIA-W is paper-only, not implemented in `sia/`.

## Future Paths

- Combo thread F1+R1+R3: *measure* that linear SIA plateaus and reward-hacks, then *fix* it with archive selection — strongest single narrative spine.
- Establishing a sentinel baseline before the next ideation cycle would ground future ideas in a coherence scorecard rather than ad-hoc code reading.
